import Game from '../classes/Game';
import Card from '../classes/Card';

describe('Game', () => {
    let game;

    beforeEach(() => {
        game = {
            game: new Game(),
            //game.playerHand.push()
          };
    });

    it.skip('should have intial game state as expected', () => {
        
      });
}); 

/*
6 DIAMONDS
6 HARTS 

QUEEN SPADES
7 DIAMONDS
*/